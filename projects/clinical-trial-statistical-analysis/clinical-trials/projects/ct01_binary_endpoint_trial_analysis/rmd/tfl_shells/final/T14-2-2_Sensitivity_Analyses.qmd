---
title: "T14-2-2 Sensitivity & Exploratory Analyses"
format: html
execute:
  echo: false
  warning: false
  message: false
---

```{r}
source("rmd/tfl_shells/final/00_tfl_setup.R")

sec_rds <- file.path(TFL$results, "secondary_models.rds")
tfl_assert_files(sec_rds)

sec <- readRDS(sec_rds)

html_path <- file.path(TFL$tables, "secondary_models.html")

int_csv <- file.path(TFL$tables, "interaction_or_by_severity.csv")
int_tbl <- if (file.exists(int_csv)) readr::read_csv(int_csv, show_col_types = FALSE) else NULL

if (file.exists(html_path)) htmltools::includeHTML(html_path)

if (!is.null(int_tbl)) knitr::kable(int_tbl, digits = 3)

```